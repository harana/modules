package com.harana.modules.shopify.models

case class MetaobjectDefinition(id: String,
                                `type`: String,
                                fieldDefinitions: List[Metafield])
